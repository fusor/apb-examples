apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tfm-postgresql
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
{% if create_pvs == true %}
  selector:
    matchLabels:
      name: tfm-postgresql
{% endif %}
